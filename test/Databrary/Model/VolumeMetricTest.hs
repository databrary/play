{-# LANGUAGE OverloadedStrings, ScopedTypeVariables #-}
module Databrary.Model.VolumeMetricTest where

import Test.Tasty.HUnit

-- import Databrary.Model.Id.Types
-- import Databrary.Model.Volume
-- import Databrary.Model.Category
-- import Databrary.Model.Metric
-- import Databrary.Model.VolumeMetric
-- import TestHarness

-- unit_assetBacked :: Assertion
-- unit_assetBacked =
  -- example
  -- typical
  -- edge cases

-- lookup on vol 1 == []
-- lookup part on vol 1 == []
-- add vol cat on vol 1, add vol metric on vol 1, lookup vol 1 == [added]
--  " w/ part cat and metric
-- add category, add metric, remove metric, lookup vol 1 == []
